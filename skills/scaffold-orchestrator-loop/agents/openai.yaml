interface:
  display_name: "Scaffold Orchestrator Loop"
  short_description: "Align the goal, then create an orchestrator control plane or fresh roadmap family."
  default_prompt: "Use $scaffold-orchestrator-loop to brainstorm and approve a roadmap strategy, then set up a repo-local orchestrator control plane from the current goal."
  tags:
    - orchestration
    - scaffolding
    - workflow
