interface:
  display_name: "Scaffold Orchestrator Loop"
  short_description: "Create an initial orchestrator control plane or open a fresh roadmap family inside a terminal one."
  default_prompt: "Use $scaffold-orchestrator-loop to set up or continue a repo-local orchestrator control plane for this repository from the current goal."
  tags:
    - orchestration
    - scaffolding
    - workflow
